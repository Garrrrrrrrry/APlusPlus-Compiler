# Fibonacci (# n):
  # a = 0;
  # b = 1;
  # c;
  # one = 1;
  ?[n eq 1]:
    return(a);
  >[n eq 2]:
    return(b);
  >[one eq 1]:
    when [n gt 1]:
      c = a add b;
      a = b;
      b = c;
      # n = n sub 1;
    ;
  ;
  return(c);
;

