# bubbleSort(#[] arr, # n):
	#[5] arr;
    # i, j, swapped;
	# sum;
	i = i add 0;
	when[i lt (n sub 1)]:
		swapped = 0 add 0;
		j = 0 add 0;
		# num = n sub i;
		# numm = num sub 1;
		when[j lt numm]:
			j = j add 1;
			# condition = j add 1;
			# arrnum = arr[condition];
			# arrnumm = arr[j];
			?[arrnumm gt arrnum]:
				# temp = arr[j];
				sum = j add 1;
				# tempnum = arr[sum];
				arr[j] = tempnum;
				arr[sum] = temp;
				swapped = 1;
			;
		;
		i = i add 1;
	;
	?[swapped eq 0]:
		stop;
	;
	return();
;